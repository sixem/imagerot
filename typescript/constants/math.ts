// Math references
const min = Math.min;
const max = Math.max;
const sin = Math.sin;
const floor = Math.floor;
const random = Math.random;
const pow = Math.pow;
const PI = Math.PI;

export { min, max, sin, floor, random, pow, PI };